instances here

<div class="row">
	<div class="small-6 large-6 columns">
			<div id="instance-list">
				<ul>
				<% @recipe.brew_instances.each do |instance| %>
					<li><%= link_to instance.created_at, recipe_brew_instance_path(instance.id) %></li>

				<% end %>
				</ul>
							</div>
	</div>
</div>